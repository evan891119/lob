#!/bin/sh
set -eu
test "$#" -eq 1 || { echo "usage: scripts/storage-init ABSOLUTE_DATA_ROOT" >&2; exit 2; }
export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}src"
python3 -m lob_recorder.cli init-storage --root "$1"
